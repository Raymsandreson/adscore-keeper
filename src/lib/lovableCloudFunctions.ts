/**
 * @deprecated Use `import { cloudFunctions } from '@/lib/functionRouter'` em vez deste arquivo.
 * 
 * Este arquivo agora é apenas um re-export do functionRouter para
 * manter compatibilidade com imports existentes sem precisar alterar 92 arquivos.
 */
export { cloudFunctions, invokeCloudFunction } from './functionRouter';
